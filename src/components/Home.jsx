import React from 'react';
import Header from './Header';

class Home extends React.Component {
    render() {
        return(
            <div className="home">
                <Header userDetails={{}} />
            </div>
        );
    }
}

export default Home;